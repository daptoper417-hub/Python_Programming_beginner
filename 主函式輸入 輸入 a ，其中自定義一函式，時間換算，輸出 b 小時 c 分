def minutes_to_hours(minutes):
    """
    將總分鐘數換算成小時與分鐘
    回傳 (小時, 分鐘)
    """
    hours = minutes // 60   # 取整數部分
    mins = minutes % 60     # 取餘數
    return hours, mins


# 主程式
a = int(input("請輸入總分鐘數 a："))

b, c = minutes_to_hours(a)

print(f"{a} 分鐘 = {b} 小時 {c} 分")
